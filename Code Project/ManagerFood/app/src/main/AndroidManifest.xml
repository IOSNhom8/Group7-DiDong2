<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trile.managerfood">

    <application
        android:allowBackup="true"
        android:icon="@drawable/cooker"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainLayoutActivity">

        </activity>
        <activity android:name=".LoginActivity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".CookActivity" />
        <activity android:name=".HealthActivity" />
        <activity android:name=".SuggestionActivity" />
        <activity android:name=".ExpertActivity" />
        <activity android:name=".DetailCookActivity" />
        <activity android:name=".DetailHearthActivity" />
        <activity android:name=".DetailSuggestActivity" />
        <activity android:name=".DetailExpertActivity" />
    </application>

</manifest>